---
command: cmd-backlog-update-fields
cli-command: "agentic-framework backlog update-fields"
module: backlog
type: update
description: "Update ticket field mappings"
related-skill: organizing-backlog
---

# Backlog Update Fields

**Command:** `agentic-framework backlog update-fields`
**Module:** Backlog Management
**Type:** update

## Description

Update ticket field mappings

## Usage

```bash
agentic-framework backlog update-fields [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework backlog update-fields
```

Use `--help` to see all available options:

```bash
agentic-framework backlog update-fields --help
```

## Related

- **Skill:** `organizing-backlog` - Provides detailed guidance for this command
