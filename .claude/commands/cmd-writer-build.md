---
command: cmd-writer-build
cli-command: "agentic-framework writer build"
module: writer
type: transform
description: "Build project: validate, generate context, analyze"
related-skill: organizing-books
---

# Writer Build

**Command:** `agentic-framework writer build`
**Module:** Fantasy Book Writer
**Type:** transform

## Description

Build project: validate, generate context, analyze

## Usage

```bash
agentic-framework writer build [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework writer build
```

Use `--help` to see all available options:

```bash
agentic-framework writer build --help
```

## Related

- **Skill:** `organizing-books` - Provides detailed guidance for this command
