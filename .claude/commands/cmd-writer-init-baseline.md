---
command: cmd-writer-init-baseline
cli-command: "agentic-framework writer init-baseline"
module: writer
type: create
description: "Initialize book baseline (world state at book start)"
related-skill: organizing-books
---

# Writer Init Baseline

**Command:** `agentic-framework writer init-baseline`
**Module:** Fantasy Book Writer
**Type:** create

## Description

Initialize book baseline (world state at book start)

## Usage

```bash
agentic-framework writer init-baseline [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework writer init-baseline
```

Use `--help` to see all available options:

```bash
agentic-framework writer init-baseline --help
```

## Related

- **Skill:** `organizing-books` - Provides detailed guidance for this command
