---
command: cmd-writer-validate-immutability
cli-command: "agentic-framework writer validate-immutability"
module: writer
type: validate
description: "Verify world fact immutability (hash verification)"
related-skill: organizing-books
---

# Writer Validate Immutability

**Command:** `agentic-framework writer validate-immutability`
**Module:** Fantasy Book Writer
**Type:** validate

## Description

Verify world fact immutability (hash verification)

## Usage

```bash
agentic-framework writer validate-immutability [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework writer validate-immutability
```

Use `--help` to see all available options:

```bash
agentic-framework writer validate-immutability --help
```

## Related

- **Skill:** `organizing-books` - Provides detailed guidance for this command
