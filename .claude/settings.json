{
  "enabledPlugins": {
    "typescript-lsp@claude-plugins-official": true,
    "feature-dev@claude-plugins-official": true,
    "agent-sdk-dev@claude-code-plugins": true
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '(^|/)\\.claude/' && echo '{\"decision\":\"block\",\"reason\":\"BLOCKED: .claude/ is a build artifact. Edit source files under framework/modules/ instead, then run sync to update .claude/.\"}' || echo '{}'",
            "statusMessage": "Checking .claude/ guard"
          }
        ]
      }
    ]
  }
}
