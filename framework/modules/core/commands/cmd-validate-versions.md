---
command: cmd-validate-versions
cli-command: "agentic-framework validate --versions"
module: core
type: validate
description: "Validate version consistency across modules"
related-skill: managing-versions
---

# Validate Versions

**Command:** `agentic-framework validate --versions`
**Module:** Framework Core
**Type:** validate

## Description

Validate version consistency across modules

## Usage

```bash
agentic-framework validate --versions [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework validate --versions
```

Use `--help` to see all available options:

```bash
agentic-framework validate --versions --help
```

## Related

- **Skill:** `managing-versions` - Provides detailed guidance for this command
