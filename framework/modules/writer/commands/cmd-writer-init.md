---
command: cmd-writer-init
cli-command: "agentic-framework writer init"
module: writer
type: create
description: "Initialize a new book project with folder structure"
related-skill: organizing-books
---

# Writer Init

**Command:** `agentic-framework writer init`
**Module:** Fantasy Book Writer
**Type:** create

## Description

Initialize a new book project with folder structure

## Usage

```bash
agentic-framework writer init [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework writer init
```

Use `--help` to see all available options:

```bash
agentic-framework writer init --help
```

## Related

- **Skill:** `organizing-books` - Provides detailed guidance for this command
