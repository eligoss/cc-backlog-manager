---
command: cmd-writer-generate-context
cli-command: "agentic-framework writer generate-context"
module: writer
type: create
description: "Generate token-budgeted context files for AI writing"
related-skill: organizing-books
---

# Writer Generate Context

**Command:** `agentic-framework writer generate-context`
**Module:** Fantasy Book Writer
**Type:** create

## Description

Generate token-budgeted context files for AI writing

## Usage

```bash
agentic-framework writer generate-context [options]
```

## Execution

When invoked, run the CLI command:

```bash
agentic-framework writer generate-context
```

Use `--help` to see all available options:

```bash
agentic-framework writer generate-context --help
```

## Related

- **Skill:** `organizing-books` - Provides detailed guidance for this command
